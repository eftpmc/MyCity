/**
 * Example Events Screen Route
 * 
 * To use this:
 * 1. Rename this file from "events.example.tsx" to "events.tsx"
 * 2. Run your app with `npm start`
 * 3. Navigate to it using router.push('/events')
 * 
 * This will create a new standalone Events screen in your app.
 */

import EventsMapScreen from '@/src/screens/EventsMapScreen';

export default EventsMapScreen;

